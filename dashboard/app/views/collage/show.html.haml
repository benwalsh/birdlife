-# Mount point for the Éist React SPA. The bootstrap blob seeds the first paint
-# (current_user, ui_lang, windows); all view data is fetched from /api/*.
#eist-app{ data: { bootstrap: @bootstrap.to_json } }
